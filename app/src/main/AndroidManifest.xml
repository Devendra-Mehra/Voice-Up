<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devendra.voiceup">

    <application
        android:name=".app.VoiceUpApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splash_screen.view.SplashScreenActivity"
            android:theme="@style/AppThemeNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".registration.sign_up.view.SignUpActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".registration.sign_in.view.SignInActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity android:name=".home.HomeActivity" />
        <activity android:name=".post.PostActivity" />
    </application>

</manifest>